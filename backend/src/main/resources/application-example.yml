review-copilot:
  model:
    # 本项目不会选择默认模型 provider。
    # 显式配置 provider 后，评审流水线会真实调用模型。
    # 支持：dashscope/dsp, openai, deepseek, anthropic, gemini, ollama, registry。
    provider: ""
    model-name: ""
    api-key: "${MODEL_API_KEY:}"
    base-url: ""
    endpoint-path: ""
